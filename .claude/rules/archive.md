# Archive Rule

`archive/` is immutable predecessor evidence. It is intentionally allowed to contain superseded terminology and coordinates. Never cite an archived file as current authority without re-admitting its claim into an active v26.8.24 surface.
