# /reconstitute

Reconstitute the requested boundary instead of incrementally preserving it.

Read `MANIFEST.json`, identify predecessor evidence, recover contracts, enumerate candidate manufacturing strategies with DfCM, select only after qualification, and state the predecessor sunset conditions. Run `python3 tools/verify.py --no-receipt` before completion.
