# /model-enterprise

Model an enterprise from public semantics first. Separate public ontology imports, enterprise ABox facts, operational contracts, manufacturing patterns, external bindings, authority policy, evidence policy, and process-event instrumentation.

For financial-enterprise examples, use the admitted FIBO profile in `ontology/fortune500-fibo-profile.ttl` rather than inventing a private duplicate vocabulary.
