# APS v26.7.31 Candidate Release Notes

## Classification

- Lifecycle: In Review
- Candidate base: `54f842c43b335db50d808c39495f2fca0e6427c9`
- Candidate branch: `agent/aps-last-7d-ggen-standards`
- Candidate date: 2026-07-30
- Promotion rule: exact-head verifier receipts only

## Scope

v26.7.31 extends the immutable v26.7.30 edition without rewriting its historical coordinate. The candidate admits the last-seven-day ggen manufacturing standard and applies Enterprise Architecture as Strategy as a machine-readable operating model for ggen.

The candidate adds:

- ontology-owned authority and deterministic projection;
- one canonical ggen Building Block kernel;
- typed refusal precedence and state-preserving replay;
- combinatorial maximalism under fail-closed gates;
- deterministic non-LLM self-play law;
- Gall-grade work-order DAGs and logical-time OCEL evidence;
- exact-head release law and BRCE-only consequential actuation;
- the four RWR operating models and four original maturity stages;
- a fenced ggen Stage 5, Evidence-Bearing Self-Governance;
- 21 conjunctive enterprise-architecture dimensions and 63 proof obligations;
- a complete root APS syntax entry point;
- deterministic source-set receipts and a WIP-closure verifier.

## Publication boundary

The committed v26.7.30 HTML and PDF remain the last immutable published projections until v26.7.31 receives exact-head promotion. Pull-request workflows are read-only. They build v26.7.31 HTML, PDF, simulation evidence, and publication receipts as immutable workflow artifacts; they do not push generated files back into the candidate branch.

Publication is therefore separated from verification:

```text
candidate source
-> exact-head verification
-> immutable workflow artifacts
-> explicit promotion authority
-> release actuation
-> published projection receipt
```

## WIP closure

The candidate removes obsolete bootstrap payloads and the legacy self-mutating publisher. It refuses:

- zero-byte canonical syntax;
- mutable action tags;
- workflow-side source repair;
- workflow-side commits or pushes;
- stale committed focused receipts;
- mdBook include stubs being judged without expansion;
- current-candidate verification overwriting historical v26.7.30 receipts.

`verify_wip_closure_v26_7_31.py` scans active canonical surfaces, workflow authority, obsolete carriers, chapter topology, and current release language. Historical conversation archives and generated mdBook output remain explicitly outside the active-WIP marker scan.

## Standing and nonclaims

Focused profile receipts may receive `ALIVE` for their source-set invariants. The complete candidate remains `UNKNOWN` or `PARTIAL_ALIVE` until exact-head GitHub Actions produce all required runtime, render, and publication artifacts.

This candidate does not claim that ggen enterprise architecture was deployed in production, that a real Fortune-5 enterprise was reorganized, that public MCP or A2A endpoints were deployed, or that any model received production actuation authority.
