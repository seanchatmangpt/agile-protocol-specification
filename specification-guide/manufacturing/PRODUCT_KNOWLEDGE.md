# Product knowledge

The mdBook product definition is no longer carried primarily by a handwritten navigation file. Book identity, chapter identity, order, labels, and paths live as admitted RDF facts. The generated navigation file exists because a consumer still requires that representation, not because Markdown is the durable source of truth.
