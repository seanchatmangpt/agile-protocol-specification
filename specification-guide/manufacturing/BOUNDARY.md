# Standing boundary

A successful exact-head run establishes only this bounded claim:

> The pinned ggen runtime and pinned ggen-marketplace mdBook pattern can deterministically manufacture the admitted APS navigation/control projection, verify its ggen receipt, and reproduce qualified output byte-identically on replay.

Complete APS self-hosting remains a larger falsifiable target.
