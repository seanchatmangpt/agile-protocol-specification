# Replay qualification

The manufacturing court distinguishes receipt verification from replay. A valid receipt binds one execution; fixed-point qualification additionally requires a second `ggen sync run` to reproduce the qualified projections byte-for-byte. Neither condition substitutes for semantic fitness of the underlying contract.
