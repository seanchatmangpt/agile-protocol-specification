# Known-pattern reuse

The mdBook surface is intentionally not novel APS mechanism. It is a known publishing/navigation pattern, so APS composes the existing `mdbook-pattern-language-pack` instead of authoring another implementation. The only APS-specific knowledge is the admitted book/navigation graph and bounded presentation extensions.
