# Work positioning

The manufacturing cell deterministically positions the work by copying only the admitted APS consumer inputs into a scratch root and mounting the exact marketplace pack at the path declared by `ggen.toml`. The live repository is not used as an ambient template search path, preventing accidental authority leakage from unrelated files.
