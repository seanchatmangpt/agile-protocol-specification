# Manufacture versus metrology

`ggen sync run` manufactures. `ggen receipt verify`, byte-identity replay, the static ecosystem court, and the repository semantic/schema court qualify different evidence classes. No manufacturer is allowed to promote its own narrative into standing merely because generation returned success.
