# Ordered manufacturing sequence

The exact sequence is part of the jig: resolve coordinates → stage admitted workpiece → manufacture → verify receipt → replay → compare bytes → run independent courts → assign bounded standing. The sequence externalizes process knowledge that would otherwise live in operator memory.
